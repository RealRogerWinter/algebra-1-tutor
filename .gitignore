# Built skill artifact duplicate (.zip is the installable one and is committed)
algebra-1-tutor.skill

# Python
__pycache__/
*.pyc
.pytest_cache/

# OS
.DS_Store
Thumbs.db

# Image-generation scratch + local API key (NEVER commit the key)
_imgwork/
*.key

# Visual-element review gallery (regenerable via build_gallery.py; the viz/ modules + the
# build tool are committed, but the generated preview page is not shipped)
docs/preview/
